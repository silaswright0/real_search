# web

Next.js UI for real search.

From the repository root:

```bash
cp .env.example .env
docker compose up --build
```

Then open http://localhost:3000.

Architecture, environment variables, and security notes live in the [root README](../README.md).
