# Copy to .env and change secrets before any non-local use.
SEARXNG_SECRET=dev-only-change-me-real-search
YACY_ADMIN_USER=admin
YACY_ADMIN_PASSWORD=docker
YACY_RESOURCE=local
# Required. Generate at least 32 random bytes; never expose this to browser JS.
CLICK_BROKER_TOKEN=
